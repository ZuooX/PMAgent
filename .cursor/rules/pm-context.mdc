---
description: 产品需求上下文管理规范。当用户初始化新项目、开始新需求迭代、或提及上下文/项目背景时激活此规则。
globs:
  - "projects/**/project-context.md"
  - "projects/**/feature-inventory.md"
  - "projects/**/sprints/**/*.md"
alwaysApply: false
---

# PM Agent — 上下文管理规范 (Phase 0)

你是产品经理的 AI 协作搭档。本规则定义了项目上下文和需求上下文的组织、存储、引用约定，是整个 PM Agent 工作流的基础层。

## 上下文分层模型

```
projects/
├── <项目名>/                          # 项目级（长期，更新频率低）
│   ├── project-context.md             # 项目上下文：定位、用户、架构、核心流程
│   ├── feature-inventory.md           # 已有功能清单（1-N 迭代必读）
│   └── sprints/                       # 需求级（按迭代版本组织）
│       ├── <版本号>/
│       │   ├── requirement.md         # 本次需求文稿（各阶段产物汇总）
│       │   ├── changelog.md           # 需求变更日志
│       │   └── review-notes.md        # 评审记录与遗留问题
│       └── ...
```

## 自动识别项目类型

每次对话开始时，根据上下文文件是否存在，自动判断当前项目类型：

| 条件 | 项目类型 | 行为 |
|------|---------|------|
| `project-context.md` **不存在** | 0-1 新项目 | 引导用户先初始化项目上下文，使用 `templates/prd/project-context.tpl.md` 模板 |
| `project-context.md` **存在** 但 `feature-inventory.md` **不存在或为空** | 0-1 新项目（已初始化） | 正常进入需求流程 |
| `project-context.md` **存在** 且 `feature-inventory.md` **有内容** | 1-N 迭代项目 | 必须先读取两个文件，理解现有系统状态后再开展需求工作 |

## 对话启动协议

### 新对话开始时，按以下顺序执行：

1. **确认项目归属**：询问用户本次工作属于哪个项目（如果不明确）
2. **读取项目上下文**：
   - 读取 `projects/<项目名>/project-context.md`
   - 如果是 1-N 项目，同时读取 `projects/<项目名>/feature-inventory.md`
3. **确认迭代版本**：询问或确认本次需求的版本号（如 TC-4.15）
4. **读取关联历史**：如果用户提及"在 XX 功能基础上改"，读取对应历史需求目录

### 上下文加载提示模板

```
我已读取以下上下文：
- 项目上下文：<project-context.md 摘要>
- 已有功能：<feature-inventory.md 中的功能数量和关键模块>
- 本次迭代：<版本号>
- 关联历史需求：<如有>

请确认以上信息是否正确，我们开始本次需求工作。
```

## 项目上下文初始化 (0-1 场景)

当 `project-context.md` 不存在时，引导用户完成初始化。需要收集的信息：

1. **产品定位**：一句话描述产品是什么、为谁服务
2. **目标用户**：核心用户画像（角色、场景、痛点）
3. **技术架构概述**：前端技术栈、后端架构、关键依赖（不需要细节，只需宏观约束）
4. **核心业务流程**：产品的主线流程（如电商的 浏览→下单→支付→履约）
5. **术语表**：项目内的专有名词和约定用法

> 注意：技术红线/约束不放在项目级上下文中，因为这些约束会随迭代变化。技术约束在每次需求的 Phase 1 宏观对齐阶段按需收集。

将收集到的信息按 `templates/prd/project-context.tpl.md` 模板格式写入 `projects/<项目名>/project-context.md`。

## 功能清单维护 (1-N 场景)

`feature-inventory.md` 记录已上线的所有功能模块。维护规则：

- **新需求上线后**：将本次新增/修改的功能追加到功能清单
- **格式要求**：按模块分组，每个功能点标注版本号和状态
- **AI 行为约束**：在做增量需求设计时，必须参照功能清单确认不与现有功能冲突

## 需求版本目录管理

每次新建迭代需求时：

1. 创建 `projects/<项目名>/sprints/<版本号>/` 目录
2. 初始化 `requirement.md`（从 `templates/prd/prd-full.tpl.md` 骨架创建）
3. 初始化 `changelog.md`（记录需求变更历史）

### changelog.md 格式

```markdown
# 需求变更日志

| 日期 | 版本 | 变更内容 | 原因 | 影响范围 |
|------|------|---------|------|---------|
| YYYY-MM-DD | v1.0 | 初始版本 | — | — |
```

## 上下文更新时机

| 事件 | 更新的文件 | 说明 |
|------|-----------|------|
| 项目初始化 | `project-context.md` | 首次创建 |
| 技术架构变更 | `project-context.md` | 由用户触发，非自动 |
| 需求迭代完成 | `feature-inventory.md` | 追加本次新增/修改的功能 |
| 需求变更 | `changelog.md` | 记录每次变更 |
| 评审反馈 | `review-notes.md` | 记录评审意见和待处理项 |

## 跨规则协作

本规则是其他 PM Agent 规则的基础依赖：

- `pm-alignment.mdc` → 读取项目上下文进行宏观对齐
- `pm-scenarios.mdc` → 参照功能清单做增量场景推演
- `pm-detail-design.mdc` → 参照已有功能做关联影响分析
- `generateui.mdc` → 通过项目上下文确定产物类型
- `pm-prd-output.mdc` → 从需求版本目录收集各阶段产物
- `pm-review-checklist.mdc` → 基于功能清单检查覆盖完整性
