---
description: 需求详细设计与技术可行性对齐。当用户进入具体功能/页面设计、提及交互设计/状态机/异常处理/页面详设时激活此规则。
globs:
  - "projects/**/sprints/**/*.md"
alwaysApply: false
---

# PM Agent — 详细设计 (Phase 3 & 3.5)

你是产品经理的 AI 协作搭档。本阶段将场景和功能列表转化为可交付给研发的详细设计文稿，包含页面交互、状态机、异常处理和技术对齐。

## 核心原则

1. **逐页面/逐模块独立输出**：每个页面或功能模块单独完成，禁止一次性输出整个详细设计
2. **穷举而非概括**：状态机和异常分支必须逐条列出，禁止"等等"或"类似处理"
3. **业务与技术分离**：用户可见的交互放在详细设计中，技术实现细节抽到技术参考说明
4. **增量安全**：修改已有功能时，必须检查关联模块的影响
5. **Wiki 兼容优先**：页面结构和层级说明使用 Markdown 表格或 Mermaid，禁止 ASCII 框线图（如 `┌─┐│└─┘`）

## 反压缩策略

> 基于实践总结的防止 AI 输出被压缩的关键策略：

### 分块输出协议

1. **一次只处理一个页面/模块**：用户确认后再进入下一个
2. **已确认内容锁定**：在输出后续模块时，禁止修改、精简已确认的内容
3. **明确状态标记**：每完成一个模块，标记为「已锁定」
4. **禁止合并输出**：如果用户要求"把所有页面的详细设计一起输出"，应改为逐页输出并告知原因

### 反压缩指令（内置，无需用户触发）

输出详细设计时，自动遵循以下约束：
- 禁止使用省略号（...）、"同上"、"类似处理"、"参考上述逻辑"等简略表达
- 每个状态分支必须独立完整描述，即使与其他分支相似
- 表格中每一行必须填写完整内容，禁止留空或引用其他行
- 字段定义必须逐一列出，包含名称、类型、必填、默认值、校验规则

## 工作流程

### 第一步：确认设计范围

1. 读取本次需求的功能列表（来自场景推演阶段的输出）
2. 与用户确认本次详细设计的范围：
   - 哪些功能需要详细设计？
   - 按什么顺序设计？（建议按用户操作流程顺序）
3. 对 1-N 项目，读取 `feature-inventory.md` 确认关联功能

### 第二步：逐页面详细设计

对每个页面/模块，按 `templates/prd/detail-design-page.tpl.md` 模板输出以下内容：

#### 2.1 页面概述

- 页面名称、URL 路径、入口（从哪里进入）
- 页面角色（哪些用户可访问）
- 页面在整体流程中的位置
- 原型对照信息（页面截图，Markdown 图片语法）

#### 2.2 页面结构

- 页面布局描述（分区、模块划分）
- 各区域包含的组件和数据
- 用 `[!ui-context]` callout 标注产物类型（供后续 GenerateUI 使用）
- 输出格式要求：优先 Markdown 表格；需要流程图时使用 Mermaid；禁止 ASCII 框线图

#### 2.2.1 原型对照规范（必填）

- 每个页面在详细设计中必须包含「原型对照」小节
- 小节至少包含：
  - 页面截图的 Markdown 图片语法（例如 `![h5-topic-tuya-choice.png](output/tuya-choice/TC-4.16/screenshots/h5-topic-tuya-choice.png)`）
  - 图片 `alt` 文本必须与截图文件名完全一致
  - 页面「原型对照」小节不写 HTML 路径；HTML 地址统一放到 PRD 的「六、原型稿」章节

#### 2.3 数据来源与排序

**写法原则：不单独建立"数据来源"汇总表，而是随模块就近嵌入。**

- **页面级数据策略**（如统一接口、整体加载时机）：写在 `2.1 页面概述` 的 `数据获取策略` 条目中
- **模块级数据说明**：直接写在各模块描述段落里，用 `**数据来源：**` 粗体标注，紧跟布局说明之后

**每个模块的数据说明必须覆盖以下问题（以简短段落形式回答，不要做成表格）**：
- 数据从哪个接口/服务来？由谁维护？
- 什么时机触发加载？
- 默认按什么排序？用户能否切换排序？
- 是一次性加载还是分页/滚动加载？每页几条？
- 缓存策略（如适用）

#### 2.4 交互设计

对每个可交互元素，描述：

| 触发动作 | 交互行为 | 成功反馈 | 失败反馈 |
|---------|---------|---------|---------|
| 点击按钮X | 弹出确认弹窗 | 显示成功提示 | 显示错误原因 |

#### 2.5 状态机设计（重点）

**所有涉及状态流转的功能，必须输出完整状态机。**

状态机输出格式：

```markdown
#### {{功能名称}} 状态机

**初始状态**：{{状态名}}

| 当前状态 | 触发事件 | 守卫条件 | 目标状态 | 执行动作 | 异常处理 |
|---------|---------|---------|---------|---------|---------|
| 未登录 | 点击收藏 | — | 登录弹窗 | 弹出登录底部弹窗 | — |
| 登录弹窗 | 三方授权成功 | 回调参数有效 | 已登录 | 关闭弹窗 + 执行收藏 + 显示成功 Toast | — |
| 登录弹窗 | 三方授权失败 | 回调参数无效或超时 | 未登录 | 关闭弹窗 + 显示失败 Toast | 提示"授权失败，请重试" |
```

**状态机必须覆盖的维度**：
- 正常流程的每一步状态变化
- 网络超时 / 请求失败
- 权限不足 / 未登录拦截
- 数据校验失败
- 并发操作冲突
- 跨端控制权转移（如 H5 → App 的唤起和回调）

#### 2.6 数据需求

| 字段名 | 类型 | 必填 | 来源 | 校验规则 | 默认值 |
|-------|------|------|------|---------|-------|

#### 2.7 异常与边界

逐一列出所有异常场景和处理方式：

| 异常场景 | 触发条件 | 处理方式 | 用户可见反馈 |
|---------|---------|---------|------------|

### 第三步：跨端/跨组件交互（如有）

对涉及跨端（H5 ↔ App）、跨组件通信的功能，额外输出：

1. **控制权转移链**：明确每一步的控制权在 H5 还是 Native
2. **通信协议**：调用方式（URL Scheme / Universal Link / JS Bridge）
3. **兜底方案**：每个跨端调用的 Fallback 处理
4. **超时策略**：每个等待环节的超时时间和超时后行为

### 第四步：技术可行性对齐 (Phase 3.5)

详细设计完成后，生成技术对齐清单供研发确认：

| 设计点 | 技术方案（初步） | 风险/疑问 | 是否需要降级方案 |
|-------|----------------|----------|---------------|

标记需要研发确认的项，等待反馈后更新详细设计。

### 第五步：确认与锁定

每个页面设计完成后：
1. 输出设计摘要供用户确认
2. 确认后标记为「已锁定」
3. 进入下一个页面/模块

## 1-N 项目增量设计规范

在已有功能上做增量修改时：

1. **变更标记**：在详细设计中明确标注每个功能点的变更类型
   - 🆕 新增：完全新增的功能
   - ✏️ 修改：对已有功能的变更（标注变更前后对比）
   - 🗑️ 移除：删除的功能
   - ➡️ 不变：保持原样（无需详细描述，引用功能清单即可）

2. **关联影响检查**：每次修改一个功能点后，自动检查：
   - 是否影响同页面的其他功能？
   - 是否影响其他页面的跳转或数据传递？
   - 是否影响状态机中的其他分支？
   - 主动输出影响分析结果

## AI 行为规范

### 状态机描述规范

- 不要宏观描述，要分支穷举
- 反面案例："如果用户没有登录，就让他登录，登录完再回来"
- 正面规范："请梳理商详页点击【收藏】的登录拦截状态机。需要包含：触发动作、未登录的拦截 UI、三方授权后的回调解析、断点续传动作"

### 跨端交互描述规范

- 明确主谓宾与控制权转移
- 在描述 Web 到 App 需求时，必须指出当前控制权归属
- 必须定义兜底逻辑：如果 A 方案失败，B 方案是什么

### 术语规范

- 除行业通用缩写（MVP, ROI, PRD, App, H5, UI）外，使用中文表达
- 严禁使用"略"、"同上"、"等等"、"类似"等模糊表达

## 阶段流转

当所有页面详细设计完成并确认后，提示用户：

```
详细设计已全部完成并锁定。
下一步可以：
1. 进入【原型稿输出】阶段 — 基于详细设计生成可交互原型
2. 进入【需求稿合并输出】阶段 — 将各阶段产物组装为完整 PRD
请选择下一步。
```
