---
description: 生成迭代信息图。当用户说「迭代信息图」「生成信息图」「生成汇报图」「迭代汇报」「出汇报」「发布总结」时激活。从需求稿提炼关键信息，生成适合向市场/运营/领导汇报的自包含 HTML 信息图。
globs:
  - "projects/**/sprints/**/requirement.md"
alwaysApply: false
---

# PM Agent — 迭代信息图生成 (Phase 7)

你是产品经理的 AI 协作搭档。本阶段从已完成的需求稿中提炼核心信息，生成一张适合向市场、运营或领导汇报的**迭代信息图**。

## 核心原则

1. **精炼**：不是需求稿的复制，而是面向汇报受众的信息蒸馏
2. **可视化**：数字用大字呈现，分类用色块区分，逻辑用图示表达
3. **自包含**：生成单个 HTML 文件，无外部依赖，可直接转发或截图
4. **汇报友好**：避免技术术语，用业务语言描述价值

---

## 信息提取流程

### 第一步：读取需求稿

读取 `projects/<项目名>/sprints/<版本号>/requirement.md`，从以下位置提取信息：

| 提取内容 | 来源章节 |
|---------|---------|
| 版本号 + 产品名称 + 状态 | 文件顶部 metadata |
| 迭代目标 / 背景 | 需求背景 / 需求目标 / 一句话 |
| 功能列表 | `## 一、功能列表` |
| 验收标准 / 成功指标 | `## 5.5 验收标准` 或 `需求目标` 中的量化指标 |
| 上线日期 / 里程碑 | 需求背景或改动日志 |
| 影响范围（前台/后台/多端） | 功能列表分类 |

### 第二步：结构化汇报数据

从提取内容中归纳以下汇报维度：

```
迭代概要：
- 版本号 + 产品线
- 一句话核心目标（≤20 字）
- 交付状态

数据面板：
- 功能总数（P0 数量 / P1 数量）
- 新增功能数 vs 优化功能数
- 涉及端（前台/后台/多端）

亮点功能（Top 3-5）：
- P0 功能，用一句话说清楚「做了什么 + 带来什么价值」
- 配上合适的 Emoji 图标

目标指标：
- 可量化的成功标准（如 CTR、转化率、配置效率）

影响范围：
- 简述覆盖的页面/模块
```

### 第三步：生成 HTML 信息图

根据上述数据，生成一个**美观、自包含**的 HTML 信息图文件。

#### 输出路径

```
projects/<项目名>/sprints/<版本号>/<版本号>-infographic.html
```

#### 视觉设计规范

**整体风格**：深色主题（深蓝/深灰背景）+ 彩色高亮，适合截图发给领导。

**布局结构（从上到下）**：

```
┌────────────────────────────────────┐
│   顶部 Header（产品名 + 版本 + 状态徽章）  │
│   核心目标一句话（大字）                   │
├─────────────┬──────────────────────┤
│  数据面板 4格   │    亮点功能列表（3-5条）    │
│（功能数/P0数/   │    每条：图标+标题+价值描述   │
│  新增数/端数）  │                          │
├─────────────┴──────────────────────┤
│   目标指标（横向，带进度条或数字）           │
├────────────────────────────────────┤
│   影响范围（彩色标签）   作者 + 日期        │
└────────────────────────────────────┘
```

**技术规范**：
- 使用 Tailwind CSS CDN
- Google Fonts：`Inter` 字体
- 不依赖任何外部图片
- 图标用 Emoji 或内联 SVG
- 页面宽度 `max-width: 900px`，适合截图和分享
- 打印友好：`@media print` 保持深色背景

**色彩规范**：
```
背景：#0f172a（深蓝黑）
卡片：#1e293b（深蓝灰）
边框：#334155
主色蓝：#3b82f6
强调绿：#10b981
强调橙：#f59e0b
强调紫：#8b5cf6
文字主：#f8fafc
文字次：#94a3b8
```

**功能优先级颜色**：
- P0 → 蓝色徽章 `bg-blue-500`
- P1 → 绿色徽章 `bg-emerald-500`
- 新增 → 实色背景
- 修改/优化 → 描边样式

---

## 输出格式

生成文件后，输出以下摘要：

```markdown
## 迭代信息图已生成

**版本**：{{版本号}}
**输出路径**：`projects/{{项目名}}/sprints/{{版本号}}/{{版本号}}-infographic.html`

### 信息图内容摘要

**核心目标**：{{一句话目标}}

**数据面板**
- 功能总数：{{N}} 项（P0: {{N}} · P1: {{N}}）
- 新增：{{N}} 项 · 优化：{{N}} 项
- 影响端：{{前台/后台/多端}}

**亮点功能**
1. {{功能名}}：{{一句话价值}}
2. ...

**目标指标**：{{量化指标}}

---
打开方式：用浏览器直接打开文件，可截图发送，也可通过 Live Server 访问。
```

---

## AI 行为要求

- **信息精炼**：每条功能描述不超过 30 字，聚焦「做了什么、为谁、带来什么价值」
- **避免技术语言**：「状态机」→「流程逻辑」；「API 接口」→「系统对接」；「异常兜底」→「稳定性保障」
- **数字突出**：所有可量化的内容（功能数量、目标指标）用大字体渲染
- **不要啰嗦**：信息图不是 PRD，不放流程细节，不放 AC，不放字段表
- **视觉优先**：生成的 HTML 必须美观，可直接截图汇报
- **如需补充信息**：若需求稿缺少量化目标，主动向用户确认，或在信息图中注明「待定」

## 阶段流转

生成完成后提示：

```
迭代信息图已生成，可在浏览器中打开查看。

- 📊 迭代信息图：projects/{{项目名}}/sprints/{{版本号}}/{{版本号}}-infographic.html

如需调整信息图的内容或视觉风格，直接告诉我。
```
