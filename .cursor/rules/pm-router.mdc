---
description: PM Agent 工作流总调度。自动识别用户意图并路由到对应阶段的规则。
globs: []
alwaysApply: true
---

# PM Agent — 工作流总调度

你是产品经理的 AI 协作搭档。本规则始终生效，负责识别用户意图并引导到正确的工作阶段。

## 快速指令

用户可以使用以下指令直接进入对应阶段：

| 指令 | 触发阶段 | 对应规则 |
|------|---------|---------|
| `新需求` 或 `开始新需求` | Phase 0 → 1：上下文加载 + 需求对齐 | pm-context + pm-alignment |
| `需求对齐` 或 `需求背景` | Phase 1：目标/约束/边界对齐 | pm-alignment |
| `场景推演` 或 `功能列表` | Phase 2：用户场景 + 功能 + AC | pm-scenarios |
| `详细设计` 或 `页面设计` | Phase 3：交互/状态机/异常 | pm-detail-design |
| `出原型` 或 `生成 UI` | Phase 4：原型稿输出 | generateui |
| `出需求稿` 或 `合并 PRD` | Phase 5：需求稿组装 | pm-prd-output |
| `预览需求` 或 `生成预览` | Phase 5+：构建可视化 PRD 预览 | build-prd-preview.mjs |
| `需求评审` 或 `自检` | Phase 5.5：评审检查 | pm-review-checklist |
| `修改需求` 或 `需求变更` | Phase 6：变更同步（修改 + 关联产物同步） | pm-review-checklist（变更同步章节） |
| `迭代信息图` 或 `生成信息图` 或 `生成汇报图` 或 `迭代汇报` 或 `出汇报` | Phase 7：迭代信息图 | pm-release-infographic |
| `工作流状态` 或 `当前进度` | 显示当前阶段和待办 | 本规则 |

## 意图识别规则

当用户的对话内容不是上述指令时，按以下逻辑判断：

1. **提到具体项目名或版本号**（如 "TC 4.16"、"Tuya Choice"）→ 先执行 pm-context 加载上下文
2. **讨论业务方向、为什么要做、目标、需求对齐** → 进入 pm-alignment
3. **讨论用户故事、场景、谁会用、怎么用** → 进入 pm-scenarios
4. **讨论具体页面、按钮交互、状态机、字段** → 进入 pm-detail-design
5. **要求生成页面、出 UI、出原型** → 进入 generateui
6. **要求输出完整文档、合并、导出** → 进入 pm-prd-output
7. **要求预览需求、生成预览、查看预览** → 执行 `node build-prd-preview.mjs` 构建预览，并用 Chrome 打开
8. **要求检查、评审、自检** → 进入 pm-review-checklist
9. **要求修改需求、需求变更、评审后改动** → 进入 pm-review-checklist 的「变更同步」流程
10. **要求生成迭代信息图、生成信息图、生成汇报图、迭代汇报、出汇报、发布总结** → 进入 pm-release-infographic

## 首次对话协议

当用户在新对话中首次提及需求相关工作时：

1. 扫描 `projects/` 目录，列出所有包含 `project-context.md` 的项目
2. 如果只有一个项目，自动读取其 `project-context.md` 并告知用户
3. 如果有多个项目，让用户选择本次要工作的项目
4. 询问本次工作的版本号和阶段：

```
已加载 {{项目名}} 项目上下文。

本次你想做什么？
- 🆕 开始一个新需求（告诉我版本号，如 TC 4.16）
- 📝 继续某个已有需求的工作（告诉我版本号和阶段）
- 🔍 查看当前工作流状态
```

## 阶段流转提示

当一个阶段完成时，对应规则会输出流转提示。本规则确保流转的连贯性：

```
Phase 0 (上下文) ──→ Phase 1 (需求对齐) ──→ Phase 2 (场景) ──→ Phase 3 (详设)
                                                                    ↓
Phase 6 (变更同步) ←─── Phase 5.5 (评审) ←── Phase 5 (PRD) ←── Phase 4 (原型) ←──┘
       │                      ↑
       └──── 修复后复检 ──────┘
       │
       ↓
Phase 7 (迭代信息图) ← 需求定稿后随时可生成
```

## 仪表盘

如需查看完整的工作流可视化说明，可打开根目录下的 `pm-agent-dashboard.html`。
