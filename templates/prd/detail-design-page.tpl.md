## {{页面名称}}

### 页面概述

- **页面路径**：{{URL 或路由}}
- **入口**：{{从哪个页面/操作进入}}
- **可访问角色**：{{用户角色列表}}
- **所属模块**：{{功能模块名}}
- **变更类型**：🆕 新增 / ✏️ 修改 / ➡️ 不变

> [!ui-context]
> - **产物类型**: {{移动端 H5 / 桌面 SaaS 后台 / 官网子页面}}
> - **目标用户**: {{C 端消费者 / B 端商家 / 平台运营}}
> - **设计风格**: {{电商消费级 / 管理后台 / 品牌官网}}

### 原型对照

> 输出规范：仅保留截图，不写原型 HTML 文件路径。
> 图片必须使用 Markdown 语法，且 alt 文本与文件名完全一致（便于浏览器插件自动替换）。

![{{页面截图文件名}}](output/{{项目名}}/{{版本号}}/screenshots/{{页面截图文件名}})

### 页面结构

<!-- 描述页面布局：分区、模块划分 -->
> 输出规范：页面结构请使用标准 Markdown 表格；如需表达流程请使用 Mermaid。
> 禁止使用 ASCII 框线图（如 `┌─┐│└─┘`），避免粘贴到 Wiki 后错位。

| 区域 | 位置 | 内容 | 组件类型 |
|------|------|------|---------|
| {{区域1}} | 顶部 | {{描述}} | {{navbar / header}} |
| {{区域2}} | 主内容区 | {{描述}} | {{table / form / card}} |

### 数据来源与排序

| 模块/区域 | 数据来源 | 触发时机 | 排序规则 | 分页/加载策略 | 缓存策略 |
|-----------|---------|---------|---------|-------------|---------|
| {{模块名}} | {{API 接口 / 本地存储 / 运营配置}} | {{页面加载 / 用户操作}} | {{字段+方向，用户可否切换}} | {{分页 / 滚动加载 / 一次性，每页N条}} | {{无 / 本地N分钟 / CDN}} |

### 交互设计

#### {{交互点1名称}}

| 触发动作 | 交互行为 | 成功反馈 | 失败反馈 |
|---------|---------|---------|---------|
| {{动作}} | {{行为}} | {{反馈}} | {{反馈}} |

#### {{交互点2名称}}

| 触发动作 | 交互行为 | 成功反馈 | 失败反馈 |
|---------|---------|---------|---------|
| {{动作}} | {{行为}} | {{反馈}} | {{反馈}} |

### 状态机

#### {{功能名称}} 状态机

**初始状态**：{{状态名}}

| 当前状态 | 触发事件 | 守卫条件 | 目标状态 | 执行动作 | 异常处理 |
|---------|---------|---------|---------|---------|---------|
| {{状态}} | {{事件}} | {{条件}} | {{状态}} | {{动作}} | {{异常}} |

### 数据需求

| 字段名 | 类型 | 必填 | 来源 | 校验规则 | 默认值 |
|-------|------|------|------|---------|-------|
| {{字段}} | {{string/number/boolean}} | 是/否 | {{API/本地/用户输入}} | {{规则}} | {{值}} |

### 异常与边界

| 异常场景 | 触发条件 | 处理方式 | 用户可见反馈 |
|---------|---------|---------|------------|
| 网络超时 | 请求超过 {{X}} 秒无响应 | {{重试/降级}} | {{Toast/提示}} |
| 权限不足 | 用户角色无访问权限 | {{跳转/提示}} | {{错误页/弹窗}} |
| 数据为空 | 查询结果为空 | 显示空状态 | {{空状态组件}} |

### 技术对齐清单

| 设计点 | 技术方案（初步） | 风险/疑问 | 需确认 |
|-------|----------------|----------|-------|
| {{设计点}} | {{方案}} | {{风险}} | 是/否 |
